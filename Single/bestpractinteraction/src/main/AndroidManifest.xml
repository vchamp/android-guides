<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.vm.guides.bestpractinteraction"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".BestPractInteractionActivity"
            android:label="Best for Interaction">
        </activity>
        <activity
            android:name=".activities.SearchActivity"
            android:label="Adding search">
        </activity>
        <activity
            android:name=".activities.SearchResultActivity"
            android:label="Search result">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>
        <activity
            android:name=".activities.EffectiveNavigationActivity"
            android:label="Navigation root">
        </activity>
        <activity
            android:name=".activities.NavSecondLevelActivity"
            android:label="Navigation 2nd level"
            android:parentActivityName="">
        </activity>
    </application>

</manifest>
